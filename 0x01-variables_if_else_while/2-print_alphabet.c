#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

	int main(void) 
{
		char letter = 'a';

		while (letter <= 'z') {
			putchar(letter); // Use putchar instead of printf
			letter++;
}

	return (0);
}
